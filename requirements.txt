# Elderly Monitoring System - Python dependencies

# --- collector (Tuya polling + storage) ---
tinytuya>=1.15
psycopg2-binary>=2.9
python-dotenv>=1.0

# --- inference / interface (charts) ---
matplotlib>=3.7

# --- api (Phase 3 web layer + alerting) ---
fastapi>=0.110
uvicorn>=0.27
bcrypt>=4.1
requests>=2.31
